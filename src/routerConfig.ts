
// This file ensures we only use one router configuration
// Import and re-export the router from router.tsx
import { router } from './router';

// Export the router as the default export for easier imports
export default router;
