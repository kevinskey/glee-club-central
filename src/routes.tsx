
// This file is being replaced by router.tsx to avoid duplication
import { router } from './router';

// Re-export the router from router.tsx to maintain compatibility 
// with any code that might be importing from this file
export { router };
export default router;
